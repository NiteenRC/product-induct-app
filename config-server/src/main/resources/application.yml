server:
  port: 8082

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NiteenRC/configurations
          default-label: master

management:
  endpoints:
    web:
      exposure:
        include: "*"